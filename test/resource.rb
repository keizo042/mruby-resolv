assert("Resolv::DNS::Resource::CNAME") do
end

assert("Resolv::DNS::Resource::NS") do
end

assert("Resolv::DNS::Resource::PTR") do
end

assert("Resolv::DNS::Resource::Generics") do
end

assert("Resolv::DNS::Resource::HINFO") do
end

assert("Resolv::DNS::Resource::MINFO") do
end

assert("Resolv::DNS::Resource::MX") do
end

assert("Resolv::DNS::Resource::SOA") do
end

assert("Resolv::DNS::Resource::TXT") do
end

assert("Resolv::DNS::Resource::ANY") do
end
